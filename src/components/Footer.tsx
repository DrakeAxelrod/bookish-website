import { Box } from "@chakra-ui/react";
import { FC } from "react";

type FooterProps = {
};

const Footer: FC<FooterProps> = () => {
  return (
    <Box as="footer">
      Footer
    </Box>
  );
};

export default Footer;
