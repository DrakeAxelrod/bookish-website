import { Box } from "@chakra-ui/react";
import { FC } from "react";

type HeaderProps = {
};

const Header: FC<HeaderProps> = () => {
  return (
    <Box as="footer">
      Header
    </Box>
  );
};

export default Header;
